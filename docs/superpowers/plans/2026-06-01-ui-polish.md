# UI 打磨 Implementation Plan（按鈕音效 + 中文化 + 共用 theme）

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 所有 UI 按鈕加程式生成點擊音、所有 UI 文字中文化、用共用 `.theme`（思源黑體 + 統一字級/字色）統一視覺並讓中文正確顯示。

**Architecture:** 新增 `UISfx` autoload（runtime 合成點擊音的 AudioStreamWAV，走 SFX bus，公開 `play_click()`）；各面板在 `_ready` 把按鈕 `pressed` 連到 `UISfx.play_click`（動態按鈕在生成時接）。新增 `ui_theme.tres`（default_font=思源黑體），在 project.godot 設為全域 `gui/theme/custom`，移除各面板重複的字級 override。中文化逐檔改寫死字串。

**Tech Stack:** Godot 4.6.1 / GDScript。測試沿用 headless `SceneTree` 模式。

**Godot 執行檔（不在 PATH）：** `C:\Download Programs\Godot_v4.6.1-stable_win64.exe\Godot_v4.6.1-stable_win64.exe`
跑測試：`& "C:\Download Programs\Godot_v4.6.1-stable_win64.exe\Godot_v4.6.1-stable_win64.exe" --headless --path game --script res://src/test/<t>.gd`

**專案硬規則：** 絕不用 `:=`（Variant 推斷是專案錯誤）；所有變數顯式型別含 `for x: T in`。.gd 用 TAB 縮排。

**測試 harness 慣例：** autoload 取真實節點 `get_root().get_node("UISfx")`，不能 `load().new()`。

---

## 參考：已確認事實
- 中文字型：`res://assets/fonts/NotoSansCJKtc-Regular.otf`（uid `uid://wy3uk1o317pj`），目前無人使用。
- 音訊 bus：Master/Music/SFX。one-shot SFX 模式見 `footstep_player.gd`（AudioStreamPlayer.bus="SFX" + play()）。
- autoload 區在 project.godot 第 20 行起；現有最後幾個：AudioSettings/SurfaceRegistry/FootstepPlayer/MusicPlayer（第 32-35 行）。`[rendering]` 在第 104 行（檔尾）。專案**無** `[gui]` 區。
- 面板按鈕接線位置（各 `_ready`）：MainMenu.gd:11-13、PauseMenu.gd:17-22、AudioSettingsPanel.gd:24、KeybindSettings.gd:27 + 動態:76、SaveLoadPanel.gd（動態 row 按鈕 + CloseButton）、QuestJournal（無按鈕，純顯示）、DialogueUI（SendButton + 動態 choice）。

## File Structure

| 檔案 | 責任 | 動作 |
|---|---|---|
| `game/src/audio/ui_sfx.gd` | UISfx autoload：合成點擊音 + play_click() | Create |
| `game/src/ui/ui_theme.tres` | 共用 Theme（思源黑體 + 字級/字色） | Create |
| `game/project.godot` | 註冊 UISfx autoload + `[gui]` gui/theme/custom | Modify |
| `game/src/ui/menus/MainMenu.{tscn,gd}` | 中文化 + 按鈕音 | Modify |
| `game/src/ui/menus/PauseMenu.{tscn,gd}` | 中文化 + 按鈕音 | Modify |
| `game/src/ui/menus/AudioSettingsPanel.{tscn,gd}` | 中文化 + 按鈕音 | Modify |
| `game/src/ui/menus/KeybindSettings.{tscn,gd}` | 中文化 + 按鈕音（含動態） | Modify |
| `game/src/ui/menus/QuestJournal.{tscn,gd}` | 中文化（無按鈕） | Modify |
| `game/src/ui/menus/HUD.tscn` | 中文化 | Modify |
| `game/src/ui/menus/SaveLoadPanel.gd` | 按鈕音（動態 + close；文字已中文） | Modify |
| `game/src/test/test_ui_sfx.gd` | UISfx headless 測試 | Create |

---

## Task 1: UISfx autoload（程式生成點擊音）

**Files:**
- Create: `game/src/audio/ui_sfx.gd`
- Modify: `game/project.godot`（autoload 區）
- Create: `game/src/test/test_ui_sfx.gd`

- [ ] **Step 1: 建立 UISfx autoload**

Create `game/src/audio/ui_sfx.gd`:

```gdscript
## UISfx — autoload
## UI 按鈕點擊音。runtime 合成一段短促衰減音（AudioStreamWAV），走 SFX bus。
## 用法：按鈕在 _ready 把 pressed 連到 play_click，例：
##   my_button.pressed.connect(UISfx.play_click)
extends Node

const SAMPLE_RATE: int = 22050
const DURATION_SEC: float = 0.045          # 短促
const FREQ_HZ: float = 880.0               # 清脆的「噠」
const AMPLITUDE: float = 0.28              # 不刺耳

var _player: AudioStreamPlayer
var _click: AudioStreamWAV

func _ready() -> void:
	_player = AudioStreamPlayer.new()
	_player.bus = "SFX"
	add_child(_player)
	_click = _build_click()

## 播放點擊音。stream 缺失時安全 no-op。
func play_click() -> void:
	if _click == null:
		return
	_player.stream = _click
	_player.play()

## 合成一段 8-bit 衰減正弦，前後加極短淡入淡出避免爆音。
func _build_click() -> AudioStreamWAV:
	var n: int = int(SAMPLE_RATE * DURATION_SEC)
	var data: PackedByteArray = PackedByteArray()
	data.resize(n)
	for i: int in range(n):
		var t: float = float(i) / float(SAMPLE_RATE)
		var env: float = exp(-t * 60.0)                       # 指數衰減
		var fade_in: float = clampf(float(i) / 64.0, 0.0, 1.0)  # ~3ms 淡入
		var sample: float = sin(TAU * FREQ_HZ * t) * env * fade_in * AMPLITUDE
		var v: int = clampi(int(sample * 127.0), -128, 127)
		data[i] = v & 0xFF                                    # 8-bit signed → byte
	var wav: AudioStreamWAV = AudioStreamWAV.new()
	wav.format = AudioStreamWAV.FORMAT_8_BITS
	wav.mix_rate = SAMPLE_RATE
	wav.stereo = false
	wav.data = data
	return wav
```

- [ ] **Step 2: 註冊 autoload**

在 `game/project.godot` 的 `[autoload]` 區，`MusicPlayer="*res://src/audio/music_player.gd"`（第 35 行）之後新增一行：

```
UISfx="*res://src/audio/ui_sfx.gd"
```

- [ ] **Step 3: 寫測試**

Create `game/src/test/test_ui_sfx.gd`:

```gdscript
## Headless 測試：UISfx 點擊音 autoload。
## 跑法：godot --headless --path game --script res://src/test/test_ui_sfx.gd
extends SceneTree

var _fail: int = 0

func _assert(cond: bool, msg: String) -> void:
	if cond:
		print("  PASS: ", msg)
	else:
		_fail += 1
		printerr("  FAIL: ", msg)

func _initialize() -> void:
	call_deferred("_run_tests")

func _run_tests() -> void:
	_test_ui_sfx()
	if _fail == 0:
		print("ALL PASS")
		quit(0)
	else:
		printerr("%d FAILED" % _fail)
		quit(1)

func _test_ui_sfx() -> void:
	print("[UISfx]")
	var sfx: Node = get_root().get_node("UISfx")
	_assert(sfx != null, "UISfx autoload 存在")
	_assert(sfx._player != null, "內部 AudioStreamPlayer 已建立")
	_assert(sfx._player.bus == "SFX", "走 SFX bus")
	_assert(sfx._click != null, "點擊音已合成")
	_assert(sfx._click is AudioStreamWAV, "stream 為 AudioStreamWAV")
	_assert(sfx._click.data.size() > 0, "波形資料非空")
	# 呼叫 play_click 不應崩
	sfx.play_click()
	_assert(true, "play_click() 執行不崩")
```

- [ ] **Step 4: 跑測試確認通過**

Run: `& "C:\Download Programs\Godot_v4.6.1-stable_win64.exe\Godot_v4.6.1-stable_win64.exe" --headless --path game --script res://src/test/test_ui_sfx.gd`
Expected: `[UISfx]` 全 PASS，`ALL PASS`，離開碼 0。

- [ ] **Step 5: Commit**

```bash
git add game/src/audio/ui_sfx.gd game/project.godot game/src/test/test_ui_sfx.gd
git commit -m "feat(audio): UISfx autoload — procedural button click sound on SFX bus

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 2: 共用 Theme 資源 + 全域套用

**Files:**
- Create: `game/src/ui/ui_theme.tres`
- Modify: `game/project.godot`（加 `[gui]` 區）

- [ ] **Step 1: 建立 theme 資源**

Create `game/src/ui/ui_theme.tres`:

```
[gd_resource type="Theme" load_steps=2 format=3 uid="uid://muzha_ui_theme"]

[ext_resource type="FontFile" uid="uid://wy3uk1o317pj" path="res://assets/fonts/NotoSansCJKtc-Regular.otf" id="1_cjk_font"]

[resource]
default_font = ExtResource("1_cjk_font")
default_font_size = 16
Label/colors/font_color = Color(0.92, 0.92, 0.92, 1)
Label/font_sizes/font_size = 14
Button/font_sizes/font_size = 15
```

- [ ] **Step 2: 設為全域預設 theme**

在 `game/project.godot` 檔尾（`[rendering]` 區之後）新增：

```

[gui]

theme/custom="res://src/ui/ui_theme.tres"
```

- [ ] **Step 3: 語法/載入檢查（headless 開機）**

Run: `& "C:\Download Programs\Godot_v4.6.1-stable_win64.exe\Godot_v4.6.1-stable_win64.exe" --headless --path game --quit-after 60`
Expected: 離開碼 0；輸出無 `SCRIPT ERROR`／`Failed to load`／`ui_theme` 相關錯誤（AI 伺服器相關訊息可忽略）。
（註：headless 仍會載入 theme 與字型資源，能抓到資源路徑/uid 錯誤。）

- [ ] **Step 4: Commit**

```bash
git add game/src/ui/ui_theme.tres game/project.godot
git commit -m "feat(ui): shared UITheme (Noto Sans CJK TC font) as global default

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 3: MainMenu 中文化 + 按鈕音 + 移除字級 override

**Files:**
- Modify: `game/src/ui/menus/MainMenu.tscn`
- Modify: `game/src/ui/menus/MainMenu.gd`

- [ ] **Step 1: .tscn 中文化 + 移除按鈕字級 override**

在 `game/src/ui/menus/MainMenu.tscn`：
- `StartButton`（:72-73）：`text = "New Game"` → `text = "新遊戲"`，刪除其下 `theme_override_font_sizes/font_size = 16` 行。
- `LoadButton`（:77-78）：`text = "Load Game"` → `text = "讀取進度"`，刪除 `theme_override_font_sizes/font_size = 16` 行。
- `QuitButton`（:82-83）：`text = "Quit"` → `text = "離開"`，刪除 `theme_override_font_sizes/font_size = 16` 行。
- 保留 `TitleLabel`「木境傳說」與其 font_size = 36（標題特意大小）。
- 保留 `SubtitleLabel`「Project Muzha」（品牌副標）與其 font_size = 14。

- [ ] **Step 2: .gd 接按鈕音**

在 `game/src/ui/menus/MainMenu.gd` 的 `_ready()`，於 `_quit_btn.pressed.connect(_on_quit)`（:13）之後新增：

```gdscript
	_start_btn.pressed.connect(UISfx.play_click)
	_load_btn.pressed.connect(UISfx.play_click)
	_quit_btn.pressed.connect(UISfx.play_click)
```

- [ ] **Step 3: 語法檢查**

Run: `& "C:\Download Programs\Godot_v4.6.1-stable_win64.exe\Godot_v4.6.1-stable_win64.exe" --headless --path game --check-only --script res://src/ui/menus/MainMenu.gd`
Expected: 無 parse error（autoload identifier 警告可忽略）。

- [ ] **Step 4: Commit**

```bash
git add game/src/ui/menus/MainMenu.tscn game/src/ui/menus/MainMenu.gd
git commit -m "feat(ui): localize MainMenu to Chinese + button click sound

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 4: PauseMenu 中文化 + 按鈕音 + 移除字級 override

**Files:**
- Modify: `game/src/ui/menus/PauseMenu.tscn`
- Modify: `game/src/ui/menus/PauseMenu.gd`

- [ ] **Step 1: .tscn 中文化 + 移除按鈕/標籤字級 override**

在 `game/src/ui/menus/PauseMenu.tscn`：
- `Title`（:44）：`text = "PAUSED"` → `text = "暫停"`。保留其 font_size = 20（標題）。
- `ZoneLabel`（:53）：`text = "Location: ---"` → `text = "地點：---"`，刪除 `theme_override_font_sizes/font_size = 12`（:54）。
- `TimeLabel`（:58）：`text = "Time: ---"` → `text = "時間：---"`，刪除 `theme_override_font_sizes/font_size = 12`（:59）。
- `ResumeButton`（:66）→「繼續」，刪 font_size 行（:67）。
- `SaveButton`（:71）→「儲存」，刪 font_size 行（:72）。
- `LoadButton`（:76）→「讀取」，刪 font_size 行（:77）。
- `SettingsButton`（:81）→「設定」，刪 font_size 行（:82）。
- `AudioButton`（:86）→「音效」，刪 font_size 行（:87）。
- `MainMenuButton`（:91）→「回主選單」，刪 font_size 行（:92）。
- `StatusLabel`（:94-99）：保留（含其綠色 modulate 與 font_size 12，狀態訊息特意小字）。

- [ ] **Step 2: .gd 中文化動態字串 + 接按鈕音**

在 `game/src/ui/menus/PauseMenu.gd`：
- `_update_info()`（:37-38）：
  ```gdscript
	_zone_label.text = "地點：" + Zones.display_name(StoryManager.current_zone)
	_time_label.text = "時間：" + StoryManager._get_time_string()
  ```
- 在 `_ready()` 末端（`_main_menu_btn.pressed.connect(_on_main_menu)`，:22 之後）新增：
  ```gdscript
	for b: Button in [_resume_btn, _save_btn, _load_btn, _settings_btn, _audio_btn, _main_menu_btn]:
		b.pressed.connect(UISfx.play_click)
  ```

- [ ] **Step 3: 語法檢查**

Run: `& "C:\Download Programs\Godot_v4.6.1-stable_win64.exe\Godot_v4.6.1-stable_win64.exe" --headless --path game --check-only --script res://src/ui/menus/PauseMenu.gd`
Expected: 無 parse error。

- [ ] **Step 4: Commit**

```bash
git add game/src/ui/menus/PauseMenu.tscn game/src/ui/menus/PauseMenu.gd
git commit -m "feat(ui): localize PauseMenu to Chinese + button click sound

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 5: AudioSettingsPanel 中文化 + 按鈕音

**Files:**
- Modify: `game/src/ui/menus/AudioSettingsPanel.tscn`
- Modify: `game/src/ui/menus/AudioSettingsPanel.gd`

- [ ] **Step 1: .tscn 中文化**

在 `game/src/ui/menus/AudioSettingsPanel.tscn`：
- `Title`（:36）：`text = "Audio"` → `text = "音效設定"`。保留 font_size 14 + 金色（標題特意樣式）。
- `CloseButton`（:50）：`text = "Close"` → `text = "關閉"`。保留 font_size 11。
- MasterRow `Label`（:73）：`"Master Volume"` → `"主音量"`，刪 font_size 12（:74）。
- MusicRow `Label`（:99）：`"Music Volume"` → `"音樂"`，刪 font_size 12（:100）。
- SFXRow `Label`（:125）：`"SFX Volume"` → `"音效"`，刪 font_size 12（:126）。
- 三個 `Value`（"100%"）保留（百分比顯示，font_size 12 + 右對齊特意保留）。
- `MuteCheck`（:146）：`"Mute All Audio"` → `"全部靜音"`，刪 font_size 12（:147）。

- [ ] **Step 2: .gd 接按鈕音**

在 `game/src/ui/menus/AudioSettingsPanel.gd` 的 `_ready()`，於 `_close_btn.pressed.connect(_on_close)`（:24）之後新增：

```gdscript
	_close_btn.pressed.connect(UISfx.play_click)
```

（滑桿/checkbox 不加點擊音——只按鈕加。）

- [ ] **Step 3: 語法檢查**

Run: `& "C:\Download Programs\Godot_v4.6.1-stable_win64.exe\Godot_v4.6.1-stable_win64.exe" --headless --path game --check-only --script res://src/ui/menus/AudioSettingsPanel.gd`
Expected: 無 parse error。

- [ ] **Step 4: Commit**

```bash
git add game/src/ui/menus/AudioSettingsPanel.tscn game/src/ui/menus/AudioSettingsPanel.gd
git commit -m "feat(ui): localize AudioSettingsPanel to Chinese + close button sound

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 6: KeybindSettings 中文化 + 按鈕音（含動態列）

**Files:**
- Modify: `game/src/ui/menus/KeybindSettings.tscn`
- Modify: `game/src/ui/menus/KeybindSettings.gd`

- [ ] **Step 1: .tscn 中文化**

在 `game/src/ui/menus/KeybindSettings.tscn`：
- `Title`（:36）：`text = "Key Bindings"` → `text = "按鍵設定"`。保留 font_size 14 + 金色。
- `CloseButton`（:66）：`text = "Close"` → `text = "關閉"`。保留 font_size 11。

- [ ] **Step 2: .gd 中文化動作名 + 狀態訊息 + 動態按鈕音**

在 `game/src/ui/menus/KeybindSettings.gd`：
- `REBINDABLE_ACTIONS`（:5-14）改為中文 value：
  ```gdscript
  const REBINDABLE_ACTIONS: Dictionary = {
  	"move_up":        "上移",
  	"move_down":      "下移",
  	"move_left":      "左移",
  	"move_right":     "右移",
  	"interact":       "互動",
  	"pause":          "暫停",
  	"toggle_map":     "地圖",
  	"toggle_journal": "任務日誌",
  }
  ```
- `_ready()`（:27 `_close_btn.pressed.connect(_on_close)` 之後）加：
  ```gdscript
	_close_btn.pressed.connect(UISfx.play_click)
  ```
- 動態 rebind 按鈕：在 `_rebuild_list()` 的 `btn.pressed.connect(_on_rebind_pressed.bind(action, btn))`（:76）之後加：
  ```gdscript
		btn.pressed.connect(UISfx.play_click)
  ```
- `_on_rebind_pressed()`（:84-85）：
  ```gdscript
	btn.text = "按任意鍵…"
	_status_label.text = "按任意鍵重新綁定"
  ```
- `_rebind_action()`（:94）：`_status_label.text = "Saved!"` → `_status_label.text = "已儲存！"`。

- [ ] **Step 3: 語法檢查**

Run: `& "C:\Download Programs\Godot_v4.6.1-stable_win64.exe\Godot_v4.6.1-stable_win64.exe" --headless --path game --check-only --script res://src/ui/menus/KeybindSettings.gd`
Expected: 無 parse error。

- [ ] **Step 4: Commit**

```bash
git add game/src/ui/menus/KeybindSettings.tscn game/src/ui/menus/KeybindSettings.gd
git commit -m "feat(ui): localize KeybindSettings to Chinese + button click sound

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 7: QuestJournal + HUD 中文化

**Files:**
- Modify: `game/src/ui/menus/QuestJournal.tscn`
- Modify: `game/src/ui/menus/QuestJournal.gd`
- Modify: `game/src/ui/menus/HUD.tscn`

- [ ] **Step 1: QuestJournal.tscn 中文化**

在 `game/src/ui/menus/QuestJournal.tscn`：
- `Title`（:44）：`text = "Quest Journal [J]"` → `text = "任務日誌 [J]"`。保留 font_size 14 + 金色。
- `ActiveTitle`（:57）：`text = "Active"` → `text = "進行中"`。保留 font_size 12 + 顏色。
- `CompletedTitle`（:74）：`text = "Completed"` → `text = "已完成"`。保留 font_size 12 + 顏色。

（QuestJournal 程式生成的 row 已用 `_add_item` 帶自訂 font_size，且為動態內容——本 task 不動 .gd 的 font_size override，僅改 .tscn 三個標題。）

- [ ] **Step 2: QuestJournal.gd 中文化 placeholder**

在 `game/src/ui/menus/QuestJournal.gd`，把兩處 `"(none)"`（:26、:39）改為 `"（無）"`。

- [ ] **Step 3: HUD.tscn 中文化**

在 `game/src/ui/menus/HUD.tscn`：
- `QuestTitle`（:52）：`text = "Quest"` → `text = "任務"`。保留 font_size 11 + 金色。
- `QuestLabel`（:61）：`text = "- ..."` → `text = "- ..."`（佔位，執行時動態覆寫，不改）。
- `ZoneLabel`/`TimeLabel` 的 `"---"` 為動態佔位，不改。

（HUD.gd 的 `"- "` 前綴是符號，保留。）

- [ ] **Step 4: 語法檢查**

Run: `& "C:\Download Programs\Godot_v4.6.1-stable_win64.exe\Godot_v4.6.1-stable_win64.exe" --headless --path game --check-only --script res://src/ui/menus/QuestJournal.gd`
Expected: 無 parse error。

- [ ] **Step 5: Commit**

```bash
git add game/src/ui/menus/QuestJournal.tscn game/src/ui/menus/QuestJournal.gd game/src/ui/menus/HUD.tscn
git commit -m "feat(ui): localize QuestJournal + HUD to Chinese

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 8: SaveLoadPanel 按鈕音（文字已中文）

**Files:**
- Modify: `game/src/ui/menus/SaveLoadPanel.gd`

- [ ] **Step 1: 接按鈕音（close + 動態 row 按鈕）**

在 `game/src/ui/menus/SaveLoadPanel.gd`：
- `_ready()` 的 `_close_btn.pressed.connect(...)` 那行之後加：
  ```gdscript
	_close_btn.pressed.connect(UISfx.play_click)
  ```
- `_make_row()` 中三個動態按鈕（存/讀/刪）各自 `pressed.connect(...)` 之後，分別加一行 `<btn>.pressed.connect(UISfx.play_click)`。具體：
  - 「存」按鈕：在它的 `save_btn.pressed.connect(...)` 之後加 `save_btn.pressed.connect(UISfx.play_click)`。
  - 「讀」按鈕：在 `load_btn.pressed.connect(...)` 之後加 `load_btn.pressed.connect(UISfx.play_click)`。
  - 「刪」按鈕：在 `del_btn.pressed.connect(...)` 之後加 `del_btn.pressed.connect(UISfx.play_click)`。

（讀檔的「讀」會 reload 場景——但 `pressed` 訊號在同幀同步發出，點擊音會先播再 reload，可接受。）

- [ ] **Step 2: 語法檢查**

Run: `& "C:\Download Programs\Godot_v4.6.1-stable_win64.exe\Godot_v4.6.1-stable_win64.exe" --headless --path game --check-only --script res://src/ui/menus/SaveLoadPanel.gd`
Expected: 無 parse error。

- [ ] **Step 3: Commit**

```bash
git add game/src/ui/menus/SaveLoadPanel.gd
git commit -m "feat(ui): button click sound on SaveLoadPanel buttons

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 9: DialogueUI 按鈕音（SendButton + 動態 choice）

**Files:**
- Modify: `game/src/ui/dialogue/DialogueUI.gd`

- [ ] **Step 1: 接 SendButton 點擊音**

在 `game/src/ui/dialogue/DialogueUI.gd` 的 `_ready()`，於 `_send_btn.pressed.connect(_on_send_pressed)`（約 :51）之後新增：

```gdscript
	_send_btn.pressed.connect(UISfx.play_click)
```

- [ ] **Step 2: 接動態 choice 按鈕點擊音**

在 `show_choices()` 建立每個 choice button 的迴圈中，`btn.pressed.connect(func() -> void: beat_choice_made.emit(i))`（約 :131）之後新增：

```gdscript
		btn.pressed.connect(UISfx.play_click)
```

- [ ] **Step 3: 語法檢查**

Run: `& "C:\Download Programs\Godot_v4.6.1-stable_win64.exe\Godot_v4.6.1-stable_win64.exe" --headless --path game --check-only --script res://src/ui/dialogue/DialogueUI.gd`
Expected: 無 parse error。

- [ ] **Step 4: Commit**

```bash
git add game/src/ui/dialogue/DialogueUI.gd
git commit -m "feat(ui): button click sound on DialogueUI send + choice buttons

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 10: 全鏈整合驗證

**Files:** （無新檔；驗證 + 回歸）

- [ ] **Step 1: 確認所有英文 UI 串已清除**

Run（應無輸出）:
```bash
grep -rn "New Game\|Load Game\|\"Quit\"\|PAUSED\|\"Resume\"\|\"Settings\"\|Main Menu\|Quest Journal\|\"Active\"\|\"Completed\"\|Key Bindings\|Master Volume\|Music Volume\|SFX Volume\|Mute All Audio\|Move Up\|Move Down\|Press a key\|Press any key\|\"Saved!\"\|\"(none)\"\|Location: \|Time: " game/src/ui
```
Expected: 無輸出（全部已中文化）。若 `Project Muzha` 出現是預期保留，不算。

- [ ] **Step 2: 跑全部測試**

Run 這些，皆須 `ALL PASS`：
```
& "C:\Download Programs\Godot_v4.6.1-stable_win64.exe\Godot_v4.6.1-stable_win64.exe" --headless --path game --script res://src/test/test_ui_sfx.gd
& "C:\Download Programs\Godot_v4.6.1-stable_win64.exe\Godot_v4.6.1-stable_win64.exe" --headless --path game --script res://src/test/test_save_system.gd
& "C:\Download Programs\Godot_v4.6.1-stable_win64.exe\Godot_v4.6.1-stable_win64.exe" --headless --path game --script res://src/test/test_quest_objectives.gd
& "C:\Download Programs\Godot_v4.6.1-stable_win64.exe\Godot_v4.6.1-stable_win64.exe" --headless --path game --script res://src/test/test_trust_gain.gd
& "C:\Download Programs\Godot_v4.6.1-stable_win64.exe\Godot_v4.6.1-stable_win64.exe" --headless --path game --script res://src/test/test_music_fade.gd
```

- [ ] **Step 3: headless 開機零錯誤**

Run: `& "C:\Download Programs\Godot_v4.6.1-stable_win64.exe\Godot_v4.6.1-stable_win64.exe" --headless --path game --quit-after 90`
Expected: 離開碼 0；無 `SCRIPT ERROR`／`Parse Error`（AI 伺服器相關訊息可忽略）。

- [ ] **Step 4: 手動驗證（編輯器，非 headless）**

開遊戲，確認：(a) 所有選單文字為中文且字型正常（思源黑體，不缺字/不 tofu）；(b) 按各按鈕有「噠」點擊音、受音效設定音量影響、靜音時無聲；(c) 主選單/暫停/存讀檔/任務日誌/設定面板外觀字型一致。

---

## Self-Review 紀錄
- **Spec 覆蓋：** §3.1 UISfx→Task 1；§3.2 theme→Task 2；§3.3 + §5 中文化→Task 3-8（逐面板）；按鈕音→Task 3-9（所有面板含動態）；§7 測試→Task 1 + Task 10。
- **型別一致：** `UISfx.play_click`（無參，可直接當 `pressed` 的 callable）、`UISfx._player`/`_click` 在測試引用一致。
- **與 spec 一致：** 只點擊音（無 hover）、程式生成、保留 [J]/Project Muzha/按鍵名、共用 theme 設思源黑體。
- **保留項目明確：** 標題大小（36/20/14）、狀態訊息小字+顏色、百分比顯示——只移除「一般按鈕/標籤」的重複字級 override，不動標題等特意樣式。
- **無 placeholder：** 各步驟含確切檔案/行號/字串/程式碼與指令。
- **動態按鈕涵蓋：** SaveLoadPanel（存/讀/刪/close）、KeybindSettings（rebind）、DialogueUI（choice）皆在生成處接音。
